## 1

undefined -> ভেরিয়েবল declare করা হয়েছে কিন্তু কোনো value assign করা হয়নি, var Hoisting এর সময় দেখা যায়।

null -> ইচ্ছাকৃতভাবে “খালি” বা “কোনো মান নেই” বোঝাতে assign করা হয়।


## 2

map() -> নতুন array return করে, যেখানে প্রতিটি element পরিবর্তিত হয়। (data transform করার জন্য)

forEach() -> শুধু loop চালায়, কিছু return করে না (undefined দেয়) (শুধু iterate করার জন্য)।
👉 map() data transform করার জন্য, forEach() শুধু iterate করার জন্য।

## 3 == এবং === এর পার্থক্য?

== -> শুধু value compare করে (type convert করে)।

=== -> value + type দুটোই compare করে। (গুড প্র্যাকটিস)


## 4 API fetch করার ক্ষেত্রে async/await এর গুরুত্ব কী?

Asynchronous code সহজ ও readable করে।

Promise handle করা সহজ হয় এবং প্রমিস চেইন হতে বিরত রাখে। 



## 5 Global, Function, Block Scope

Global Scope -> যেকোনো জায়গা থেকে access করা যায়।

Function Scope -> শুধু সেই function এর ভিতরে ব্যবহারযোগ্য।

Block Scope -> {} এর ভিতরে সীমাবদ্ধ (let, const দিয়ে)।
